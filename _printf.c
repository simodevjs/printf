#include "main.h"
/**
 * _printf- is a task from ALX it is a function for printing
 * @format: its an identifier to seek.
 * Return: 0 if succed.
 */

int _printf(const char *format, ...)
{
	return (0);
}
